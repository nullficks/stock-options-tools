server {
    listen 80;
    server_name options.brennpunkt-index.de;
    
    root /var/www/stock-options-tools;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1M;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS server (uncomment after SSL setup)
# server {
#     listen 443 ssl http2;
#     server_name options.brennpunkt-index.de;
#     
#     ssl_certificate /etc/letsencrypt/live/options.brennpunkt-index.de/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/options.brennpunkt-index.de/privkey.pem;
#     
#     root /var/www/stock-options-tools;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ =404;
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript;
# }
